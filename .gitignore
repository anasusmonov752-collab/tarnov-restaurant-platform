# Dependencies
node_modules/

# Data (JSON files & uploads - server creates these automatically)
# Boshida "/" bor — FAQAT loyiha ildizidagi data/ papkasi.
# Busiz src/data/ ham bloklanadi va deployda server yiqiladi.
/data/
uploads/

# Environment
.env
.env.local
# .env.example — namuna fayl, gitga tushishi KERAK (maxfiy qiymat yo'q)
!.env.example

# Lokal ish fayllari (menyu importi, cookie, debug dump'lari)
cookies.txt
ck*.txt
*_temp.json
rlist.json
all_q.json

# Ichida kirish ma'lumotlari bor lokal hujjat — repo OCHIQ, gitga tushmasin
TARNOV_PLATFORM_HUJJAT.md

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Claude Code tooling (contains local launch config with secrets)
.claude/
